# COVID and Inlfuenza Coinfection Model

# Installation

Install pip modules using ```python3 setup.py install```.

## What does the model do?

## Web server

The app can be ran locally by running:

``` bokeh serve --show --port 5001  app.py```

which will open the app in your web browser.

The app itself is hosted at [whalleyt.pythonanywhere.com ](whalleyt.pythonanywhere.com ).